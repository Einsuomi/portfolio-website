# build output
dist/
.vercel/
# generated types
.astro/

# dependencies
node_modules/

# local-only working dir — verify-ui JSON reports, screenshots, scratch notes (public repo)
logs/

# Lighthouse CI local report output (@lhci/cli writes here on every local run)
.lighthouseci/

# design reference material — local only, third-party copyrighted content (public repo)
reference/

# impeccable live-mode tool state
.impeccable/

# designer self-screenshot scratch output — local only
.shots/

# Playwright MCP scratch output (screenshots, console/page dumps) — local only
.playwright-mcp/

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*


# environment variables
.env
.env.local
.env.production

# macOS-specific files
.DS_Store

# jetbrains setting folder
.idea/

# retired-version snapshots — local backup copies (also in git history)
backups/

# Synced wiki — local-only build input for the chatbot corpus. Includes personal
# (non-confidential) pages we must NOT publish to this public repo. The CURATED
# corpus is compiled from the allowlist into knowledge.generated.ts, which IS
# committed (only the professional sources) and is what Vercel builds from.
src/content/wiki/
