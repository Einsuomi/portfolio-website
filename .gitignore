# build output
dist/
.vercel/
# generated types
.astro/

# dependencies
node_modules/

# agent dispatch logs — local only, may quote private context (public repo)
logs/

# design reference material — local only, third-party copyrighted content (public repo)
reference/

# impeccable live-mode tool state
.impeccable/

# designer self-screenshot scratch output — local only
.shots/

# Playwright MCP scratch output (screenshots, console/page dumps) — local only
.playwright-mcp/

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*


# environment variables
.env
.env.local
.env.production

# macOS-specific files
.DS_Store

# jetbrains setting folder
.idea/

# retired-version snapshots — local backup copies (also in git history)
backups/
